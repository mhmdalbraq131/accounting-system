import React, { useEffect, useState } from "react";
import { createRoot } from "react-dom/client";
import "./styles.css";
import { getPilgrims, getPrograms, getSettings, updateSetting, type Pilgrim, type Program, type Setting } from "./api";

const modules = [["dashboard","لوحة التحكم","▦"],["travel","الحج والعمرة","🕋"],["pilgrims","الحجاج والمعتمرون","👥"],["visas","التأشيرات","🪪"],["customers","العملاء","◉"],["suppliers","الموردون","◇"],["finance","الصندوق والبنوك والمحافظ","▣"],["vouchers","السندات","▤"],["expenses","المصروفات","−"],["accounts","الحسابات","≡"],["reports","التقارير","▥"],["users","المستخدمون والصلاحيات","⚙"],["settings","الإعدادات","⚙"]] as const;

function App(){
 const [active,setActive]=useState("dashboard"),[settings,setSettings]=useState<Setting[]>([]),[programs,setPrograms]=useState<Program[]>([]),[pilgrims,setPilgrims]=useState<Pilgrim[]>([]),[error,setError]=useState("");
 const title=modules.find(([id])=>id===active)?.[1]??"لوحة التحكم";
 useEffect(()=>{Promise.allSettled([getSettings(),getPrograms(),getPilgrims()]).then(([s,p,g])=>{if(s.status==='fulfilled')setSettings(s.value);if(p.status==='fulfilled')setPrograms(p.value);if(g.status==='fulfilled')setPilgrims(g.value);});},[]);
 async function saveSetting(key:string,value:string){try{const saved=await updateSetting(key,value);setSettings(old=>old.map(s=>s.key===key?saved:s));setError("");}catch(e){setError(e instanceof Error?e.message:"تعذر حفظ الإعداد");}}
 return <div className="shell" dir="rtl"><aside className="sidebar"><div className="brand"><div className="brand-mark">م</div><div><strong>نظام المحاسبة</strong><span>وكالة الحج والعمرة</span></div></div><nav>{modules.map(([id,label,icon])=><button key={id} className={active===id?"nav-item active":"nav-item"} onClick={()=>setActive(id)}><span className="nav-icon">{icon}</span><span>{label}</span></button>)}</nav><div className="sidebar-foot">نسخة مستقلة • API جاهز للتكامل لاحقًا</div></aside><section className="content"><header className="topbar"><div><h1>{title}</h1><p>نظام إدارة ومحاسبة متكامل لوكالة الحج والعمرة والسفر</p></div><div className="top-actions"><button className="icon-btn">⌕</button><button className="icon-btn">🔔</button><div className="user-chip"><span>م</span><div><b>مدير النظام</b><small>Administrator</small></div></div></div></header>{error&&<div className="error-banner">{error}</div>}{active==='dashboard'&&<Dashboard programs={programs} pilgrims={pilgrims} onNavigate={setActive}/>} {active==='settings'&&<SettingsPage settings={settings} onSave={saveSetting}/>} {active==='travel'&&<ProgramsPage programs={programs}/>} {active==='pilgrims'&&<PilgrimsPage pilgrims={pilgrims}/>} {!['dashboard','settings','travel','pilgrims'].includes(active)&&<ModulePlaceholder title={title}/>}</section></div>;
}
function Dashboard({programs,pilgrims,onNavigate}:{programs:Program[];pilgrims:Pilgrim[];onNavigate:(id:string)=>void}){const sales=programs.reduce((n,p)=>n+Number(p.sale_price||0),0),profit=programs.reduce((n,p)=>n+Number(p.sale_price||0)-Number(p.supplier_cost||0),0);return <main className="page"><div className="welcome"><div><span className="eyebrow">نظرة عامة</span><h2>مرحبًا بك في نظام المحاسبة</h2><p>كل العمليات المالية والتشغيلية للوكالة في مكان واحد.</p></div><button className="primary" onClick={()=>onNavigate('travel')}>＋ إدارة برامج الحج والعمرة</button></div><div className="stats"><Stat label="إجمالي قيمة البرامج" value={sales.toFixed(2)} unit="ريال يمني"/><Stat label="عدد البرامج" value={String(programs.length)} unit="برنامج"/><Stat label="الحجاج والمعتمرون" value={String(pilgrims.length)} unit="مسجل"/><Stat label="الربح المتوقع للبرامج" value={profit.toFixed(2)} unit="ريال يمني"/></div><div className="grid-main"><section className="panel"><div className="panel-head"><div><h3>حالة برامج الحج والعمرة</h3><p>آخر البرامج المسجلة</p></div><button className="link" onClick={()=>onNavigate('travel')}>عرض الكل</button></div>{programs.length?<TablePrograms programs={programs.slice(0,5)}/>:<Empty icon="🕋" title="لا توجد برامج حتى الآن" text="أضف برنامج عمرة أو حج للبدء."/>}</section><section className="panel quick"><div className="panel-head"><div><h3>وصول سريع</h3><p>العمليات اليومية</p></div></div><div className="quick-grid"><Quick label="برامج الحج والعمرة" icon="🕋" onClick={()=>onNavigate('travel')}/><Quick label="الحجاج والمعتمرون" icon="👥" onClick={()=>onNavigate('pilgrims')}/><Quick label="التأشيرات" icon="🪪" onClick={()=>onNavigate('visas')}/><Quick label="الإعدادات" icon="⚙" onClick={()=>onNavigate('settings')}/></div></section></div></main>}
function Stat({label,value,unit}:{label:string;value:string;unit:string}){return <div className="stat"><span>{label}</span><strong>{value}</strong><small>{unit}</small></div>}
function Quick({label,icon,onClick}:{label:string;icon:string;onClick:()=>void}){return <button className="quick-btn" onClick={onClick}><span>{icon}</span><b>{label}</b><small>فتح الوحدة</small></button>}
function TablePrograms({programs}:{programs:Program[]}){return <div className="table-wrap"><table><thead><tr><th>الكود</th><th>البرنامج</th><th>النوع</th><th>سعر البيع</th><th>تكلفة المورد</th></tr></thead><tbody>{programs.map(p=><tr key={p.id}><td>{p.code}</td><td>{p.name_ar}</td><td>{p.program_type==='hajj'?'حج':'عمرة'}</td><td>{Number(p.sale_price).toFixed(2)}</td><td>{Number(p.supplier_cost).toFixed(2)}</td></tr>)}</tbody></table></div>}
function ProgramsPage({programs}:{programs:Program[]}){return <main className="page"><section className="panel"><div className="panel-head"><div><h2>برامج الحج والعمرة</h2><p>البرامج والأسعار وتكاليف الموردين والطاقة الاستيعابية</p></div><button className="primary">＋ إضافة برنامج</button></div>{programs.length?<TablePrograms programs={programs}/>:<Empty icon="🕋" title="لا توجد برامج مضافة" text="أضف أول برنامج عمرة أو حج للبدء."/>}</section></main>}
function PilgrimsPage({pilgrims}:{pilgrims:Pilgrim[]}){return <main className="page"><section className="panel"><div className="panel-head"><div><h2>الحجاج والمعتمرون</h2><p>بيانات المسافرين وحالات التأشيرات</p></div><button className="primary">＋ إضافة مسافر</button></div>{pilgrims.length?<div className="table-wrap"><table><thead><tr><th>الاسم</th><th>الجواز</th><th>الجنسية</th><th>الهاتف</th><th>حالة التأشيرة</th></tr></thead><tbody>{pilgrims.map(p=><tr key={p.id}><td>{p.full_name}</td><td>{p.passport_number}</td><td>{p.nationality||'—'}</td><td>{p.phone||'—'}</td><td>{p.visa_status}</td></tr>)}</tbody></table></div>:<Empty icon="👥" title="لا يوجد حجاج أو معتمرون" text="أضف أول مسافر لبدء إدارة ملفات العملاء."/>}</section></main>}
function SettingsPage({settings,onSave}:{settings:Setting[];onSave:(key:string,value:string)=>Promise<void>}){const groups=settings.reduce<Record<string,Setting[]>>((a,s)=>{(a[s.category]??=[]).push(s);return a;},{});return <main className="page"><section className="panel"><div className="panel-head"><div><h2>إعدادات النظام</h2><p>إعدادات الوكالة والمالية والحج والعمرة والواجهة</p></div></div>{Object.entries(groups).map(([category,items])=><div className="settings-group" key={category}><h3>{category==='company'?'بيانات الوكالة':category==='financial'?'الإعدادات المالية':category==='travel'?'إعدادات الحج والعمرة':'إعدادات الواجهة'}</h3><div className="settings-grid">{items.map(s=><SettingField key={s.key} setting={s} onSave={onSave}/>)}</div></div>)}</section></main>}
function SettingField({setting,onSave}:{setting:Setting;onSave:(key:string,value:string)=>Promise<void>}){const [value,setValue]=useState(setting.value);const [saving,setSaving]=useState(false);useEffect(()=>setValue(setting.value),[setting.value]);async function blur(){if(value===setting.value||!setting.is_editable)return;setSaving(true);await onSave(setting.key,value);setSaving(false)}return <label><span>{setting.description_ar||setting.key}{saving?' • جارٍ الحفظ…':''}</span><input value={value} disabled={!setting.is_editable} onChange={e=>setValue(e.target.value)} onBlur={blur}/></label>}
function Empty({icon,title,text}:{icon:string;title:string;text:string}){return <div className="empty"><div>{icon}</div><b>{title}</b><span>{text}</span></div>}
function ModulePlaceholder({title}:{title:string}){return <main className="page"><section className="panel module"><div className="module-icon">▦</div><h2>{title}</h2><p>هيكل الوحدة موجود، وسيتم ربط العمليات المالية والتقارير والصلاحيات بها تدريجيًا مع الحفاظ على النواة المحاسبية.</p><button className="primary">＋ إضافة جديد</button></section></main>}
createRoot(document.getElementById("root")!).render(<React.StrictMode><App/></React.StrictMode>);
