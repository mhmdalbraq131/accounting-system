import React,{useEffect,useMemo,useState} from "react";
import {getAccounts,getSettings,updateSetting,type Setting} from "./api";
const ACCOUNT_KEYS=new Set(["travel_revenue_account_id","travel_cost_account_id","visa_revenue_account_id","visa_cost_account_id","flight_revenue_account_id","flight_cost_account_id","bus_revenue_account_id","bus_cost_account_id","visit_revenue_account_id","visit_cost_account_id","work_visa_revenue_account_id","work_visa_cost_account_id"]);
const groupLabel=(c:string)=>({company:"بيانات الوكالة",financial:"الإعدادات المالية",travel:"إعدادات الحج والعمرة",travel_accounting:"ربط الحج والعمرة والتأشيرات بالمحاسبة",service_accounting:"ربط الطيران والباصات والزيارات وفيز العمل بالمحاسبة",interface:"الواجهة"}[c]||c);
export default function SettingsPage(){const [settings,setSettings]=useState<Setting[]>([]),[accounts,setAccounts]=useState<any[]>([]),[error,setError]=useState(""),[saving,setSaving]=useState("");async function refresh(){try{const [s,a]=await Promise.all([getSettings(),getAccounts()]);setSettings(s);setAccounts(a);setError("")}catch(e){setError(e instanceof Error?e.message:"تعذر تحميل الإعدادات")}}useEffect(()=>{refresh()},[]);const grouped=useMemo(()=>settings.reduce<Record<string,Setting[]>>((acc,s)=>{(acc[s.category]??=[]).push(s);return acc},{}),[settings]);async function save(key:string,value:string){setSaving(key);try{const updated=await updateSetting(key,value);setSettings(xs=>xs.map(s=>s.key===key?updated:s));setError("")}catch(e){setError(e instanceof Error?e.message:"تعذر حفظ الإعداد")}finally{setSaving("")}}return <main className="page"><div className="welcome"><div><span className="eyebrow">الإعدادات</span><h2>إعدادات وكالة مهراس</h2><p>جميع الإعدادات الإدارية والمالية وربط الحسابات الافتراضية من مكان واحد.</p></div></div>{error&&<div className="error-banner">{error}</div>}{Object.entries(grouped).map(([category,items])=><section className="panel settings-panel" key={category}><div className="panel-head"><div><h3>{groupLabel(category)}</h3><p>يتم حفظ التعديل مباشرة عند مغادرة الحقل.</p></div></div><div className="settings-grid">{items.map(s=><label key={s.key}><span>{s.description_ar||s.key}{saving===s.key?" • جارٍ الحفظ…":""}</span>{ACCOUNT_KEYS.has(s.key)?<select value={s.value} disabled={!s.is_editable} onChange={e=>save(s.key,e.target.value)}><option value="">اختر الحساب...</option>{accounts.filter(a=>a.is_active).map(a=><option key={a.id} value={a.id}>{a.code} - {a.name_ar}</option>)}</select>:s.value_type==="boolean"?<select value={s.value} disabled={!s.is_editable} onChange={e=>save(s.key,e.target.value)}><option value="true">نعم</option><option value="false">لا</option></select>:<input value={s.value} disabled={!s.is_editable} onChange={e=>setSettings(xs=>xs.map(x=>x.key===s.key?{...x,value:e.target.value}:x))} onBlur={e=>e.target.value!==settings.find(x=>x.key===s.key)?.value?save(s.key,e.target.value):undefined}/>}</label>)}</div></section>)}</main>}
